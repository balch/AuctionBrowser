<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/menu_search"
        android:title="@string/search_hint"
        android:icon="@android:drawable/ic_menu_search"
        app:showAsAction="always"
        app:actionViewClass="android.widget.SearchView"
        />

    <group android:checkableBehavior="single">
        <item android:id="@+id/menu_sort_best_match"
            android:title="@string/auction_sort_col_best_match"
            app:showAsAction="never"
            />

        <item android:id="@+id/menu_sort_ending_soonest"
            android:title="@string/auction_sort_col_ending_soonest"
            app:showAsAction="never"
            />

        <item android:id="@+id/menu_sort_lowest_price"
            android:title="@string/auction_sort_col_lowest_price"
            app:showAsAction="never"
            />
    </group>

</menu>